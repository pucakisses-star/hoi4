# Deliberately empty. Overrides the vanilla file of the same name.
#
# This mod replaces HOI4's idea categories in common/idea_tags/00_idea.txt with
# a Victorian set, so roughly 1,243 vanilla ideas never load. Vanilla content
# then references ideas that are not there, and the crash has consistently
# landed in whichever vanilla file was processed last - first the DLC
# on_actions, then, once those were overridden, the DLC events.
#
# The content dropped here is WW2-era material with no place in an 1868 world.
# The mod ships its own focus trees, events and decisions.
