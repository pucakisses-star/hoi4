# Deliberately empty. Overrides the vanilla file of the same name.
#
# This mod replaces HOI4's idea categories in common/idea_tags/00_idea.txt with
# a Victorian set, which means roughly 1,243 vanilla ideas no longer load. The
# vanilla DLC on_actions then call swap_ideas and add_timed_idea on ideas that
# are not there, and every crash so far has ended inside one of these files.
#
# replace_path="common/on_actions" in the descriptor would be the tidier way to
# do this, but it only takes effect in the OUTER .mod file, which lives outside
# this repository. Overriding by filename works purely from mod content, so it
# applies as soon as the folder is copied - no descriptor edit needed.
#
# The content dropped here is DLC WW2 material with no place in an 1868 world.
on_actions = {
}
